blockChain = ['Genesis']


def add_transaction(amount):
    """ This Function adds transaction as nested list to Existing BlockChain

        Arguments:
            amount: value to be added to the BlockChain
     """
    return blockChain.append([blockChain[-1], amount])


age = 18

condition = True if age > 18 else False
print(con)
